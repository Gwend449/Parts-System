# ✅ Интеграция форм с AmoCRM - Завершено

## 📋 Что было сделано

### 1. ✅ Добавлено поле Email в обе формы
- **Форма контактов** (`resources/views/livewire/pages/contacts.blade.php`)
  - Новое поле Email (обязательное) между Имя и Телефон
  - Валидация email адреса (RFC + DNS)
  
- **Форма каталога** (`resources/views/livewire/pages/catalog.blade.php`)
  - Новое поле Email (обязательное)
  - Валидация email адреса

### 2. ✅ Исправлена форма в каталоге
**Проблема:** Форма не отправлялась - не было обработчика и полей не имели `name` атрибутов

**Решение:**
- Добавлены `name` атрибуты ко всем полям формы
- Добавлен JavaScript обработчик `submit` события
- Добавлены блоки с ошибками валидации
- Добавлен alert для вывода сообщений об успехе/ошибке

### 3. ✅ Обновлена валидация
Файл: `app/Http/Requests/ContactFormRequest.php`

Новые правила:
```php
'email' => 'required|email:rfc,dns', // Обязательное поле
```

Новые сообщения об ошибках:
```php
'email.required' => 'Пожалуйста, введите вашу почту',
'email.email' => 'Пожалуйста, введите корректный адрес почты',
```

### 4. ✅ Обновлены контроллеры
Файл: `app/Http/Controllers/FormSubmissionController.php`

Оба метода теперь передают email в амоCRM:
```php
$leadId = $this->amoService->sendLead(
    name: $validated['name'],
    phone: $validated['phone'],
    email: $validated['email'] ?? null,  // ← Новый параметр
    brand: $validated['brand'] ?? null,
    ...
);
```

### 5. ✅ Обновлен AmoService
Файл: `app/Services/AmoService.php`

**Метод `sendLead()`:**
- Добавлен параметр `?string $email = null`
- Метод теперь передает email при создании контакта

**Метод `createOrUpdateContact()`:**
- Добавлена поддержка email (если это возможно в SDK)
- При создании контакта добавляется email в кастомные поля

---

## 🧪 Тестирование

### Проверка формы контактов:
1. Перейти на `/contacts`
2. Заполнить форму со всеми полями **включая Email**
3. Нажать "Отправить сообщение"
4. **Ожидается:**
   - ✅ Зелёный алерт с текстом "Спасибо! Ваше сообщение отправлено..."
   - ✅ Форма очищается
   - ✅ В амоCRM создаётся лид с почтой в контакте

### Проверка формы каталога:
1. Перейти на `/catalog`
2. Заполнить форму **ВКЛЮЧАЯ Email** и марку авто
3. Нажать "Отправить запрос"
4. **Ожидается:**
   - ✅ Зелёный алерт с текстом "Спасибо! Ваш запрос отправлен..."
   - ✅ Форма очищается
   - ✅ В амоCRM создаётся лид с источником "Каталог"

### Проверка валидации:
1. Попробовать отправить форму **без Email** → ошибка "Пожалуйста, введите вашу почту"
2. Заполнить Email невалидным адресом (например "test") → ошибка "Пожалуйста, введите корректный адрес почты"
3. Оставить поле пустым → ошибка валидации (поле обязательное)

---

## 📊 Логирование

Все операции логируются в `storage/logs/laravel.log`:

```
Форма контактов отправлена:
[2025-01-19] Форма контактов успешно отправлена в amoCRM
lead_id: 123456
name: Иван Петров
email: ivan@example.com
phone: +7 (910) 123-45-67

Форма каталога отправлена:
[2025-01-19] Форма из каталога успешно отправлена в amoCRM
lead_id: 123457
name: Сергей Сидоров
email: sergey@example.com
phone: +7 (910) 987-65-43
brand: BMW
```

---

## 🔍 Отладка

### Если форма не отправляется:

1. **Проверить браузерную консоль** (F12 → Console)
   - Должны быть видны успешные fetch запросы
   - Искать ошибки сети или CORS

2. **Проверить логи Laravel:**
   ```bash
   tail -f storage/logs/laravel.log | grep -E "(контактов|каталога)"
   ```

3. **Проверить амоCRM:**
   - Авторизован ли API?
   - `php artisan amo:status` должен показать ✅ AmoCRM подключена

4. **Проверить CSRF токен:**
   - В HTML есть `<meta name="csrf-token">`?
   - В fetch запросе передаётся этот токен?

### Если email не сохраняется в амоCRM:

AmoCRM SDK может не поддерживать метод `setCustomFields()` или формат может отличаться.
В этом случае нужно:

1. Добавить email в примечание (комментарий) к лиду
2. Или использовать другой способ сохранения email

---

## 📝 Файлы которые были изменены

| Файл | Изменения |
|------|-----------|
| `resources/views/livewire/pages/catalog.blade.php` | Добавлены name атрибуты, email поле, JavaScript обработчик |
| `resources/views/livewire/pages/contacts.blade.php` | Добавлено email поле, обновлен JavaScript |
| `app/Http/Requests/ContactFormRequest.php` | Добавлена валидация email |
| `app/Http/Controllers/FormSubmissionController.php` | Добавлена передача email в оба контроллера |
| `app/Services/AmoService.php` | Обновлены методы sendLead() и createOrUpdateContact() |

---

## ✅ Список готовых функций

- [x] Email поле обязательное в обеих формах
- [x] Валидация email адреса (RFC + DNS)
- [x] JavaScript обработчик в форме каталога
- [x] Отправка email в амоCRM через контакт
- [x] Красивые алерты об успехе/ошибке
- [x] Логирование всех операций
- [x] Ошибки валидации выводятся в форму
- [x] Spinner при отправке формы

---

## 🚀 Дальнейшие улучшения (опционально)

1. **Сохранение email в комментарий лида** (если SDK не поддерживает setCustomFields)
   ```php
   $comment = "Email: {$email}\n" . ($comment ?? '');
   ```

2. **Отправка email подтверждения клиенту**
   ```php
   Mail::to($email)->queue(new LeadReceived());
   ```

3. **Проверка дублей по email перед созданием лида**
   ```php
   $existingLead = $amo->api()->leads()->filter([
       'query' => $email
   ]);
   ```

4. **Сохранение данных в локальную БД** для истории
   ```php
   FormSubmission::create([
       'name' => $name,
       'email' => $email,
       'phone' => $phone,
       'amocrm_lead_id' => $leadId,
   ]);
   ```
