# 📦 Инструкции по развертыванию на production

## 🔄 Изменения в этой версии

### 1. ✅ Удаление картинок для моторов
**Статус:** Готово к продакшену  
**Что изменилось:**
- Добавлен новый field тип `engine_media_gallery` в `resources/views/vendor/backpack/crud/fields/`
- Обновлен контроллер `EnginesCrudController` для использования нового field типа
- Добавлены маршруты для удаления медиа файлов

**Файлы для развертывания:**
```
resources/views/vendor/backpack/crud/fields/engine_media_gallery.blade.php
app/Http/Controllers/Admin/EnginesCrudController.php (обновлен)
routes/backpack/custom.php (обновлены маршруты)
```

### 2. ✅ Исправлена ошибка "base:::email Login" на странице входа
**Статус:** Готово к продакшену  
**Что изменилось:**
- Добавлены русские языковые файлы для Backpack
- Добавлены переводы ключей 'email' и 'username'

**Файлы для развертывания:**
```
resources/lang/vendor/backpack/ru/base.php (новый)
resources/lang/vendor/backpack/ru/crud.php (новый)
```

---

## 🚀 Процесс развертывания

### Шаг 1: Подготовка на development

```bash
# Убедитесь, что все изменения закоммичены
git status

# Проверьте, что Vite собран для production
npm run build

# Проверьте, что тесты проходят (если есть)
php artisan test
```

### Шаг 2: Публикация на production сервер

#### Вариант A: Через Git (рекомендуется)

```bash
# На production сервере:
cd /path/to/app

# Получить последние изменения
git pull origin main  # или ваша production ветка

# Установить/обновить зависимости
composer install --optimize-autoloader --no-dev

# Установить npm зависимости
npm install --production

# Собрать фронтенд
npm run build

# Выполнить миграции (если есть новые)
php artisan migrate --force

# Очистить кэши
php artisan cache:clear
php artisan view:clear
php artisan route:clear
php artisan config:clear

# (Опционально) перезагрузить приложение в очереди
php artisan queue:restart
```

#### Вариант B: Вручную (файлы)

1. **Скопируйте файлы:**
   ```bash
   # Копируйте следующие файлы на production:
   
   # Новый field type
   resources/views/vendor/backpack/crud/fields/engine_media_gallery.blade.php
   
   # Обновленный контроллер
   app/Http/Controllers/Admin/EnginesCrudController.php
   
   # Новые языковые файлы
   resources/lang/vendor/backpack/ru/base.php
   resources/lang/vendor/backpack/ru/crud.php
   
   # Обновленные маршруты (в файле routes/backpack/custom.php)
   ```

2. **Выполните команды на продакшене:**
   ```bash
   php artisan cache:clear
   php artisan view:clear
   ```

### Шаг 3: Проверка на production

```bash
# 1. Откройте админку и проверьте форму входа
# http://yourdomain.com/admin
# ✓ Проверьте, что поле "Email" отображается корректно (не "base:::email")

# 2. Откройте редактирование мотора
# http://yourdomain.com/admin/engines
# ✓ Создайте новый мотор или отредактируйте существующий
# ✓ Загрузьте картинку
# ✓ Проверьте, что картинка отобразилась
# ✓ Проверьте, что кнопка удаления (🗑️) появляется при наведении

# 3. Проверьте удаление картинки
# ✓ Нажмите на кнопку удаления
# ✓ Картинка должна удалиться без перезагрузки страницы
# ✓ Сохраните мотор

# 4. Проверьте фронтенд приложения
# http://yourdomain.com
# ✓ Проверьте, что все стили загружаются
# ✓ Проверьте, что изображения каталога видны
```

### Шаг 4: Откат в случае проблем

```bash
# Если что-то пошло не так, вернитесь к предыдущей версии:
git revert HEAD  # или git reset --hard <previous_commit>

# Очистите кэши
php artisan cache:clear
php artisan view:clear
```

---

## 🛠️ Проверочный список перед развертыванием

- [ ] Все изменения в `git`
- [ ] Запущен `npm run build` и нет ошибок
- [ ] Все тесты проходят (если есть)
- [ ] Протестировано локально с `npm run dev`
- [ ] Протестировано локально с `npm run build`
- [ ] Резервная копия production БД
- [ ] Время развертывания согласовано (если нужно)
- [ ] На production есть достаточно дискового пространства

---

## 📋 Файлы, которые были изменены

```
Новые файлы:
✓ resources/views/vendor/backpack/crud/fields/engine_media_gallery.blade.php
✓ resources/lang/vendor/backpack/ru/base.php
✓ resources/lang/vendor/backpack/ru/crud.php

Обновленные файлы:
✓ app/Http/Controllers/Admin/EnginesCrudController.php
✓ routes/backpack/custom.php

Документация:
✓ LOCAL_SETUP.md
✓ DEPLOYMENT_GUIDE.md (этот файл)
```

---

## 🔐 Безопасность

✅ **Все AJAX запросы защищены CSRF токеном**  
✅ **Удаление медиа проверяет права доступа через middleware**  
✅ **Все файлы загружаются с валидацией типа и размера**

---

## ⚡ Оптимизация

**Для ускорения загрузки на production:**

```bash
# Оптимизируйте автозагрузку composer
composer install --optimize-autoloader --no-dev

# Кэшируйте конфиги
php artisan config:cache
php artisan route:cache

# Отправляйте медиа файлы на CDN (если необходимо)
# Обновите URL в .env переменной MEDIA_URL
```

---

## 📞 В случае проблем

Если после развертывания возникают проблемы:

1. **Проверьте логи:** `storage/logs/laravel-*.log`
2. **Проверьте браузер:** F12 → Console tab
3. **Очистите все кэши:** `php artisan cache:clear && php artisan view:clear`
4. **Перезагрузите nginx/Apache:**
   ```bash
   # nginx
   sudo systemctl reload nginx
   
   # Apache
   sudo systemctl reload apache2
   ```

---

## 📝 Версия

- **Дата развертывания:** 30 декабря 2025
- **Версия приложения:** v1.0 + медиа управление + исправление локализации
- **Backpack CRUD:** v6.8+
- **Laravel:** v12.0+
