# 🎯 БЫСТРАЯ ИНСТРУКЦИЯ: Отправка форм в AmoCRM

## ✅ ЧТО ГОТОВО

Три формы теперь отправляют лиды в AmoCRM с email:

1. **Форма контактов** — страница `/contacts`
2. **Форма каталога** — страница `/catalog` (боковая панель)  
3. **Форма запроса цены** — модальное окно при клике на двигатель

---

## 📋 СТРУКТУРА ДАННЫХ

Каждая форма содержит поля:
- ✅ **Имя** (обязательное)
- ✅ **Email** (обязательное, валидируется RFC + DNS)
- ✅ **Телефон** (обязательное)
- ℹ️ **Марка авто** (опционально, только в каталоге)
- 💬 **Комментарий** (опционально)

---

## 🚀 ТЕСТИРОВАНИЕ

### 1️⃣ Форма контактов

```
URL: http://localhost:11000/contacts
Нужно заполнить: Имя, Email, Телефон, (Комментарий опционально)
Кнопка: "Отправить сообщение"
```

### 2️⃣ Форма каталога

```
URL: http://localhost:11000/catalog
Нужно заполнить: Имя, Email, Телефон, Марка авто, (Комментарий опционально)
Кнопка: "Отправить запрос" (в правой панели)
```

### 3️⃣ Форма запроса цены

```
URL: http://localhost:11000/catalog или любая страница с моторами
Кликнуть: На кнопку у двигателя
Заполнить: Имя, Email, Телефон
Кнопка: "Отправить запрос" (в модальном окне)
```

---

## ✨ ЧТО ПРОИСХОДИТ

### При успешной отправке:
✅ Форма очищается  
✅ Появляется зелёный алерт: "Спасибо! Ваше сообщение отправлено..."  
✅ В AmoCRM создаётся новый **Лид** с контактом  
✅ Email сохраняется в контакт  
✅ Источник лида указан (Контакты / Каталог / Запрос цены)

### При ошибке:
❌ Появляется красный алерт  
❌ Ошибки валидации показаны красным под полями  
❌ Форма не очищается (чтобы пользователь исправил)

---

## 🔴 ТИПИЧНЫЕ ОШИБКИ ВАЛИДАЦИИ

| Ошибка | Что не так |
|--------|-----------|
| "Пожалуйста, введите вашу почту" | Поле Email пустое |
| "Пожалуйста, введите корректный адрес почты" | Email неправильного формата (например: `test` вместо `test@example.com`) |
| "Пожалуйста, введите ваше имя" | Поле Имя пустое |
| "Пожалуйста, введите ваш телефон" | Поле Телефон пустое или неправильный формат |

---

## 🔍 ПРОВЕРКА В AMOCRM

После отправки формы:

1. Откройте AmoCRM
2. Перейдите в **Лиды**
3. Новый лид будет вверху списка
4. Кликните на лид
5. Проверьте:
   - ✅ Имя клиента
   - ✅ Email в контакте
   - ✅ Телефон в контакте
   - ✅ Источник лида (внизу на правой панели)
   - ✅ Комментарий/описание (если указано)

---

## 🐛 ОТЛАДКА

### Если форма не отправляется:

**Шаг 1: Проверить браузерную консоль**
```
F12 → Console tab
Ищите ошибки красного цвета
```

**Шаг 2: Проверить ответ сервера**
```
F12 → Network tab
Найти последний POST запрос
Посмотреть Response (статус 201 — успешно, 422 — ошибка валидации, 500 — ошибка сервера)
```

**Шаг 3: Проверить логи Laravel**
```bash
tail -f storage/logs/laravel.log
```

### Если email не сохраняется в AmoCRM:

AmoCRM SDK может требовать другой формат для email.  
Проверьте логи в `storage/logs/laravel.log` на строки вроде:
```
[WARNING] Не удалось добавить телефон/email в контакт
```

Если видите такую ошибку — need to check AmoCRM SDK documentation для правильного формата.

---

## 📊 ЛОГИРОВАНИЕ

Все действия логируются в `storage/logs/laravel.log`:

```
[2025-01-19 10:30:45] laravel.INFO: Форма контактов успешно отправлена в amoCRM {"lead_id":12345,"name":"Иван Петров","email":"ivan@example.com","phone":"+7-910-123-45-67"}

[2025-01-19 10:31:12] laravel.INFO: Форма из каталога успешно отправлена в amoCRM {"lead_id":12346,"name":"Сергей","email":"sergey@mail.ru","phone":"+7-920-555-66-77","brand":"BMW"}
```

Ошибки логируются с полным trace:
```
[2025-01-19 10:32:00] laravel.ERROR: Ошибка при отправке формы контактов в amoCRM {"error":"Connection refused","trace":"..."}
```

---

## 📝 ФАЙЛЫ ДЛЯ РАЗРАБОТЧИКОВ

| Файл | Описание |
|------|----------|
| `app/Http/Controllers/FormSubmissionController.php` | Обработка POST запросов от форм |
| `app/Http/Requests/ContactFormRequest.php` | Валидация данных формы |
| `app/Services/AmoService.php` | Отправка лидов в AmoCRM API |
| `app/Livewire/EngineCostModal.php` | Компонент модального окна |
| `resources/views/livewire/pages/contacts.blade.php` | HTML форма контактов + JavaScript |
| `resources/views/livewire/pages/catalog.blade.php` | HTML форма каталога + JavaScript |
| `resources/views/livewire/engine-cost-modal.blade.php` | HTML модального окна |
| `routes/web.php` | API routes для форм |

---

## 💡 ПРИМЕЧАНИЯ

- **Email обязателен** во всех трёх формах (требование клиента)
- **Валидация** выполняется и на фронтенде (браузер) и на бэкенде (сервер)
- **CSRF токен** автоматически проверяется Laravel
- **Все операции асинхронные** (используется fetch API без перезагрузки страницы)
- **Спиннер показывается** во время отправки формы

---

## ❓ ЧАСТЫЕ ВОПРОСЫ

**Q: Где будут сохраняться данные?**  
A: Только в AmoCRM. Локально данные не сохраняются (можно добавить позже в БД).

**Q: Можно ли отправить форму дважды?**  
A: Да, каждый раз будет создан новый лид.

**Q: Куда будет отправлен email?**  
A: Email клиента будет сохранён как контактная информация лида в AmoCRM (подтверждение пока не отправляется).

**Q: Работает ли с мобильными браузерами?**  
A: Да, форма адаптивная и работает везде.

**Q: Можно ли изменить текст ошибок?**  
A: Да, в файле `app/Http/Requests/ContactFormRequest.php` массив `messages()`.
