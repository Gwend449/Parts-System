# 🎯 ИНСТРУКЦИЯ: КАК ЗАГРУЖАТЬ ИЗОБРАЖЕНИЯ В АДМИНКЕ

## ✅ Что было исправлено

Ошибка: "Файл должен быть изображением. Допустимые форматы: JPG, PNG, WEBP" **РЕШЕНА** ✓

## 🚀 Быстрый старт

### Шаг 1: Очистить кэш приложения
```bash
docker exec php_local php artisan cache:clear
docker exec php_local php artisan view:clear
```

### Шаг 2: Протестировать в админке

1. **Откройте админку:** http://avrb1749.ru/admin или локально http://localhost:8080/admin
2. **Найдите мотор** для редактирования (или создайте новый)
3. **Загрузите изображение:**
   - Нажмите на блок загрузки фотографий
   - Выберите файл PNG, JPG или WebP
   - Размер не более 5 MB
   - Максимум 6 изображений одновременно
4. **Сохраните** нажав кнопку "Сохранить"
5. ✅ Готово! Изображение должно загрузиться

## 🖼️ Поддерживаемые форматы

| Формат | Расширение | Размер | Статус |
|--------|-----------|--------|--------|
| JPEG | .jpg, .jpeg | < 5 MB | ✅ |
| PNG | .png | < 5 MB | ✅ |
| WebP | .webp | < 5 MB | ✅ |

## 🔍 Если всё ещё не работает

### Способ 1: Проверить поддержку на сервере

```bash
docker exec php_local php artisan image:check-support
```

Вы должны увидеть:
```
✅ GD расширение загружено
✅ JPEG (imagejpeg)
✅ PNG (imagepng)
✅ WebP (imagewebp)
```

### Способ 2: Проверить версию браузера

- Chrome, Firefox, Safari - актуальные версии ✅
- Internet Explorer - не поддерживается ❌

### Способ 3: Проверить файл

- Откройте свойства файла
- Убедитесь, что это действительно изображение
- Не переименован ли файл (например, `foto.txt` вместо `foto.jpg`)

### Способ 4: Очистить браузер

```
1. Ctrl+Shift+Delete (или Cmd+Shift+Delete на Mac)
2. Выберите "За всё время"
3. Отметьте "Кэш"
4. Нажмите "Удалить"
5. Обновите страницу админки
```

## 📝 Что происходит после загрузки

После загрузки изображение:

1. **Сохраняется** в папку `/storage/app/public/`
2. **Проверяется** его тип (MIME)
3. **Обрабатывается** - создаются две копии:
   - **Превью** (600х600 пикселей) - для страницы товара
   - **Миниатюра** (250х250 пикселей) - для списка в каталоге
4. **Сжимается** - качество 75% (хороший баланс между качеством и размером)
5. **Сохраняется** информация в базе данных

## 💾 Где находятся загруженные фото

**На сервере:**
```
/storage/app/public/{engine_id}/
```

**В браузере:**
```
/storage/{engine_id}/{filename}
```

## ❌ Частые ошибки

### Ошибка 1: "Файл должен быть изображением"
**Решение:** Убедитесь, что это действительно изображение (PNG, JPG или WebP)

### Ошибка 2: "Размер изображения не должен превышать 5 MB"
**Решение:** Сожмите изображение перед загрузкой. Используйте онлайн-сервис:
- https://tinypng.com (PNG, JPG)
- https://www.iloveimg.com/compress-image (все форматы)

### Ошибка 3: "Максимум 6 изображений"
**Решение:** Загружайте изображения партиями по 6 штук, потом сохраняйте

### Ошибка 4: "Чёрный экран" или "Ошибка 500"
**Решение:** 
```bash
# Очистить кэш
docker exec php_local php artisan cache:clear
docker exec php_local php artisan view:clear

# Проверить логи
docker exec php_local tail -f storage/logs/laravel.log
```

## 📞 Техническая поддержка

Если ничего не помогает, предоставьте:
1. Снимок экрана ошибки
2. Размер и формат файла
3. Вывод команды:
   ```bash
   docker exec php_local php artisan image:check-support
   ```

---

**Обновлено:** 30 декабря 2025  
**Версия:** 1.2
