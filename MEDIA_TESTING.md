# ⚡ БЫСТРОЕ ТЕСТИРОВАНИЕ СИСТЕМЫ

## 🚀 Проверьте что работает (5 минут)

### 1. Откройте админку и выберите мотор

```
/admin/engines
```

Выберите существующий мотор или создайте новый.

### 2. Заполните основные данные

- Маркировка (slug)
- Название (title)
- Цена (price)
- Марка (brand)
- OEM

Нажмите **"Сохранить"**

### 3. Прокрутите вниз и найдите "Управление фотографиями"

Должен быть блок с:
- Текстом "Загруженные фотографии (0)" или список фото
- Кнопкой "Выбрать файлы"
- Кнопкой "Загрузить фото"

### 4. Загрузьте фотографию

1. Нажмите "Выбрать файлы"
2. Выберите JPG/PNG/WEBP (max 5 MB)
3. Должен появиться предпросмотр
4. Нажмите "Загрузить фото"

**✓ ОК если:**
- Появилось зеленое сообщение "Фотографии загружены успешно!"
- Фото появилось в галерее
- Под фото видно название и размер

**✗ ПРОБЛЕМА если:**
- Нет зеленого сообщения
- Фото не появилось
- Вместо фото - placeholder

### 5. Проверьте фото в галерее

Должны видеть:
- Миниатюра фото (не placeholder)
- Размер файла (MB)
- Название файла

### 6. Удалите фото

Нажмите кнопку 🗑️ на фото.

**✓ ОК если:**
- Появилось зеленое сообщение "Фотография удалена: ..."
- Фото исчезло из галереи

**✗ ПРОБЛЕМА если:**
- Нет сообщения об удалении
- Фото все еще видно (нужна перезагрузка)

---

## 🔍 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Уведомления не появляются?

```bash
# Проверьте что сессия работает
php artisan tinker
>>> session()->put('test', 'value')
>>> session()->get('test')
```

### Фото не загружаются?

```bash
# Проверьте логи
tail -50 storage/logs/laravel.log | grep -i media

# Проверьте что storage link работает
ls -la public/storage
```

### Конверсии не создаются?

```bash
# Проверьте БД
sqlite3 database/database.sqlite
SELECT COUNT(*) FROM media;
SELECT file_name, generated_conversions FROM media LIMIT 1\G
```

---

## 📊 ЧТО ПРОВЕРИТЬ В ЛОГАХ

```bash
tail -f storage/logs/laravel.log
```

Хорошие сообщения:
```
[2026-01-07 ...] INFO: EngineMediaManager loaded 1 images for engine 97
[2026-01-07 ...] INFO: Starting media upload for engine 97
[2026-01-07 ...] INFO: Adding media file
[2026-01-07 ...] INFO: Media added successfully
```

Плохие сообщения:
```
[2026-01-07 ...] ERROR: Error uploading media: ...
[2026-01-07 ...] ERROR: Could not find conversion
```

---

## 💻 АВТОМАТИЗИРОВАННАЯ ПРОВЕРКА

```bash
#!/bin/bash

echo "🔍 Проверка системы управления фото..."
echo ""

# 1. PHP
echo "1️⃣  PHP версия:"
php -v | head -1

# 2. Laravel
echo ""
echo "2️⃣  Laravel окружение:"
cat .env | grep APP_ENV

# 3. Database
echo ""
echo "3️⃣  Таблица media:"
sqlite3 database/database.sqlite "SELECT COUNT(*) as count FROM media;"

# 4. Storage
echo ""
echo "4️⃣  Storage symlink:"
[ -L public/storage ] && echo "✓ OK" || echo "✗ NOT FOUND"

# 5. Livewire
echo ""
echo "5️⃣  Livewire компонент:"
grep -q "class EngineMediaManager" app/Livewire/EngineMediaManager.php && echo "✓ OK" || echo "✗ NOT FOUND"

# 6. View
echo ""
echo "6️⃣  View файл:"
[ -f resources/views/livewire/engine-media-manager.blade.php ] && echo "✓ OK" || echo "✗ NOT FOUND"

# 7. ImageMagick
echo ""
echo "7️⃣  ImageMagick:"
which convert > /dev/null && echo "✓ OK" || echo "✗ NOT INSTALLED"

echo ""
echo "✅ Проверка завершена!"
```

Сохраните как `check-media.sh` и запустите:
```bash
chmod +x check-media.sh
./check-media.sh
```

---

## 🎯 ФРАГМЕНТ ТЕСТА

Вставьте в `routes/web.php`:

```php
Route::get('/test-media', function() {
    $engine = \App\Models\Engine::first();
    if (!$engine) {
        return 'No engine found';
    }
    
    return [
        'engine_id' => $engine->id,
        'media_count' => $engine->getMedia('images')->count(),
        'images' => $engine->getMedia('images')->map(fn($m) => [
            'id' => $m->id,
            'name' => $m->file_name,
            'url' => $m->getUrl(),
            'thumb' => $m->getUrl('thumb'),
            'preview' => $m->getUrl('preview'),
        ]),
    ];
});
```

Откройте: `/test-media`

Должно показать JSON с медиа информацией.

---

## 📝 КОНТРОЛЬНЫЙ СПИСОК

- [ ] Страница админки загружается
- [ ] Видно поле "Управление фотографиями"
- [ ] Кнопка "Выбрать файлы" работает
- [ ] Выбранные файлы показывают предпросмотр
- [ ] После загрузки появляется зеленое сообщение
- [ ] Фото появляется в галерее (не placeholder)
- [ ] Размер фото показан правильно
- [ ] Кнопка удаления работает
- [ ] После удаления появляется сообщение
- [ ] Фото исчезает из галереи
- [ ] При перезагрузке страницы - фото остаются

**Если все пункты ✓ - система работает идеально!**

---

**Версия:** 1.0  
**Дата:** 7 января 2026 г.
