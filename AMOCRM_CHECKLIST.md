# ✅ ЧЕК-ЛИСТ: Применение исправления

## 🎯 ЦЕЛЬ
Исправить ошибку "/amocrm/install" которая выдавала "Здесь ничего нет"

---

## 📋 ЭТАП 1: ПОДГОТОВКА (2 минуты)

- [ ] Убедиться что на сервере есть интернет (требуется для миграций)
- [ ] Проверить что текущий branch это `dev`
  ```bash
  git branch
  ```
- [ ] Посмотреть статус git
  ```bash
  git status
  ```
- [ ] Обновить код с git
  ```bash
  git pull origin dev
  ```

---

## 📋 ЭТАП 2: ЗАПУСК МИГРАЦИЙ (1 минута)

### На локальной машине (localhost):

- [ ] Убедиться что БД работает
  ```bash
  php artisan tinker
  > exit
  ```

- [ ] Запустить миграции
  ```bash
  php artisan migrate
  ```

- [ ] Проверить что таблица создана
  ```bash
  php artisan tinker
  > DB::table('amocrm_oauth_states')->get()
  > exit
  ```

### На продакшене (Docker):

- [ ] Запустить миграции в контейнере
  ```bash
  docker compose exec app php artisan migrate --force
  ```

- [ ] Проверить что таблица создана
  ```bash
  docker compose exec app php artisan tinker
  > DB::table('amocrm_oauth_states')->count()
  > exit
  ```

---

## 📋 ЭТАП 3: ОЧИСТИТЬ КЭШ (1 минута)

- [ ] Очистить кэш конфигурации
  ```bash
  php artisan cache:clear
  ```

- [ ] Перестроить кэш
  ```bash
  php artisan config:cache
  ```

- [ ] Очистить кэш представлений
  ```bash
  php artisan view:clear
  ```

- [ ] Очистить всё разом
  ```bash
  php artisan cache:clear && php artisan config:cache
  ```

---

## 📋 ЭТАП 4: ПРОВЕРИТЬ КОНФИГУРАЦИЮ (1 минута)

- [ ] Убедиться что .env содержит все необходимые переменные:
  ```bash
  grep -E "AMOCRM_|SESSION_" .env
  ```

- [ ] Должно быть:
  ```env
  AMOCRM_CLIENT_ID=...
  AMOCRM_CLIENT_SECRET=...
  AMOCRM_REDIRECT_URI=...
  AMOCRM_SUBDOMAIN=...
  SESSION_DRIVER=database
  SESSION_SECURE_COOKIE=true (на продакшене)
  SESSION_SAME_SITE=None
  ```

- [ ] Если чего-то не хватает - добавить в .env

- [ ] Перезагрузить конфиг
  ```bash
  php artisan config:cache
  ```

---

## 📋 ЭТАП 5: ТЕСТИРОВАНИЕ ЛОКАЛЬНО (3 минуты)

### Проверка #1: Состояние приложения

- [ ] Запустить сервер
  ```bash
  php artisan serve
  ```

- [ ] Проверить статус AmoCRM
  ```bash
  # В новом терминале
  php artisan amo:status
  ```

- [ ] Должно быть либо:
  - ✅ "AmoCRM подключена!" (если уже авторизовано)
  - ✅ Или ссылка на авторизацию (если первый раз)

### Проверка #2: Авторизация

- [ ] Перейти на `http://localhost:8000/amocrm/install`

- [ ] Должны увидеть:
  - ✅ Перенаправление на страницу AmoCRM
  - ✅ Или форму авторизации AmoCRM

- [ ] Если видишь ошибку:
  - ❌ "Здесь ничего нет"
  - ❌ "Ошибка безопасности"
  - 👉 Смотри раздел "ОТЛАДКА" ниже

### Проверка #3: Логирование

- [ ] Открыть логи в новом терминале
  ```bash
  tail -f storage/logs/laravel.log
  ```

- [ ] Должны быть сообщения:
  - ✅ "AmoCRM OAuth: запрос на авторизацию"
  - ✅ "AmoCRM OAuth: state сгенерирован"
  - ✅ "AmoCRM OAuth: перенаправление на авторизацию"

---

## 📋 ЭТАП 6: ПОЛНАЯ АВТОРИЗАЦИЯ (5 минут)

- [ ] Использовать ngrok (если нужен callback):
  ```bash
  ngrok http 8000
  ```

- [ ] Обновить .env (если используешь ngrok):
  ```env
  AMOCRM_REDIRECT_URI=https://abc123.ngrok.io/amocrm/callback
  ```

- [ ] Перезагрузить конфиг
  ```bash
  php artisan config:cache
  ```

- [ ] Перейти на `http://localhost:8000/amocrm/install` (или ngrok URL)

- [ ] Авторизоваться с помощью:
  - Логина от AmoCRM
  - Пароля от AmoCRM

- [ ] После авторизации должно быть:
  - ✅ Сообщение "amoCRM успешно подключена!"
  - ✅ Токен в БД

- [ ] Проверить токен в БД:
  ```bash
  php artisan tinker
  > \App\Models\AmocrmToken::first()
  ```

---

## 📋 ЭТАП 7: ТЕСТИРОВАНИЕ НА ПРОДАКШЕНЕ (5 минут)

### На Docker (если используешь docker-compose):

- [ ] Запустить контейнеры
  ```bash
  docker compose up -d
  ```

- [ ] Запустить миграции
  ```bash
  docker compose exec app php artisan migrate --force
  ```

- [ ] Очистить кэш
  ```bash
  docker compose exec app php artisan cache:clear
  docker compose exec app php artisan config:cache
  ```

- [ ] Проверить статус
  ```bash
  docker compose exec app php artisan amo:status
  ```

- [ ] Посмотреть логи
  ```bash
  docker compose logs -f app | grep -i amocrm
  ```

- [ ] Перейти на `https://avrb1749.ru/amocrm/install`

- [ ] Авторизоваться (если нужно)

- [ ] Проверить логи в контейнере
  ```bash
  docker compose exec app tail -f storage/logs/laravel.log
  ```

---

## 📋 ЭТАП 8: ФИНАЛЬНАЯ ПРОВЕРКА (2 минуты)

### На любой машине:

- [ ] Проверить все таблицы в БД
  ```bash
  php artisan tinker
  > DB::table('amocrm_oauth_states')->count()
  > DB::table('amocrm_tokens')->count()
  > DB::table('sessions')->count()
  > exit
  ```

- [ ] Проверить что нет ошибок в коде
  ```bash
  php artisan migrate --step --seed
  ```

- [ ] Проверить что routes зарегистрированы
  ```bash
  php artisan route:list | grep amocrm
  ```

- [ ] Должны быть:
  - ✅ `/amocrm/install`
  - ✅ `/amocrm/callback`

---

## 🚨 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Ошибка: "SQLSTATE[42S01]: Table 'amocrm_oauth_states' already exists"

```bash
# Таблица уже существует, просто сбрось:
php artisan migrate:refresh --seed

# Или вручную:
php artisan tinker
> DB::statement('DROP TABLE IF EXISTS amocrm_oauth_states')
> exit

# Затем запусти миграции:
php artisan migrate
```

### Ошибка: "Здесь ничего нет" ВСЕ ЕЩЕ ЕСТЬ

```bash
# 1. Посмотреть логи
tail -f storage/logs/laravel.log | grep -i amocrm

# 2. Проверить конфиг
php artisan tinker
> config('amocrm')

# 3. Проверить таблицы
> DB::table('amocrm_oauth_states')->count()
> DB::table('amocrm_tokens')->count()
> exit

# 4. Очистить БД
> DB::table('amocrm_oauth_states')->truncate()
> DB::table('sessions')->truncate()
> exit

# 5. Перезагрузить всё
php artisan cache:clear && php artisan config:cache
```

### Ошибка: "State параметр истек"

```bash
# State действителен 15 минут - просто повтори попытку
# Или очисти старые:
php artisan amo:cleanup
```

### Ошибка: "Ошибка безопасности при авторизации"

```bash
# Проверить время на сервере:
date

# Если время рассинхронизировано:
# 1. На Linux: ntpdate -s time.nist.gov
# 2. На Docker: отредактировать time в контейнере
# 3. На Mac: автоматически синхронизируется

# Проверить логи:
tail -f storage/logs/laravel.log | grep -i oauth
```

---

## ✅ ПОСЛЕ УСПЕШНОГО ВНЕДРЕНИЯ

- [ ] Создать коммит
  ```bash
  git add .
  git commit -m "fix: AmoCRM OAuth state handling - store in DB instead of session"
  ```

- [ ] Запушить изменения
  ```bash
  git push origin dev
  ```

- [ ] Удалить локальные логи (опционально)
  ```bash
  rm storage/logs/laravel.log*
  ```

- [ ] Очистить временные файлы
  ```bash
  php artisan cache:clear
  ```

---

## 📊 РЕЗУЛЬТАТЫ

### ДО исправления:
- ❌ `/amocrm/install` выдает "Здесь ничего нет"
- ❌ State параметр теряется при редиректе
- ❌ Авторизация не работает при HTTPS

### ПОСЛЕ исправления:
- ✅ `/amocrm/install` открывается без ошибок
- ✅ Перенаправляет на AmoCRM для авторизации
- ✅ Callback обменивает код на токен
- ✅ Токен сохраняется в БД
- ✅ Приложение готово использовать AmoCRM API

---

## 📞 ПОДДЕРЖКА

Если остались вопросы:

1. **Посмотри логи:**
   ```bash
   tail -f storage/logs/laravel.log
   ```

2. **Проверь статус:**
   ```bash
   php artisan amo:status
   ```

3. **Прочитай документацию:**
   - `AMOCRM_FIX_COMPLETE.md` - подробное объяснение
   - `AMOCRM_VISUAL_EXPLANATION.md` - диаграммы и примеры
   - `AMOCRM_CHANGES.md` - список всех изменений

4. **Смотри примеры:**
   - `app/Services/AmoExamples.php` - примеры использования API
   - `app/Console/Commands/CheckAmoStatus.php` - как проверить статус

---

**⏱️ Ожидаемое время: 15-20 минут**

**🎉 Результат: Рабочая авторизация AmoCRM!**
