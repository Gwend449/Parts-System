# 🔧 ЗАПУСК ПРОЕКТА

## ❌ Ошибка: "ничего не работает на localhost"

**Причина:** Docker daemon не запущен, Laravel контейнеры не работают.

---

## ✅ РЕШЕНИЕ

### Шаг 1: Запустить Docker Desktop

**На Mac:**
1. Открой Applications
2. Найди "Docker"
3. Запусти Docker.app
4. Подожди пока загрузится (~30 сек)
5. Проверь иконку Docker в меню сверху (должна быть зеленая)

**Или в терминале:**
```bash
# Запустить Docker daemon
open /Applications/Docker.app

# Подождать ~30 секунд пока загрузится
sleep 30

# Проверить что работает
docker ps
```

---

### Шаг 2: Запустить проект через Docker Compose

**Вариант A: Используя docker-compose.local.yml (рекомендуется для локальной разработки)**

```bash
# Убедись что ты в папке проекта
cd /Users/nlyapin/Templates/Parts-System

# Запусти контейнеры
docker compose -f docker-compose.local.yml up -d

# Или если старая версия docker-compose (v1):
docker-compose -f docker-compose.local.yml up -d
```

**Вариант B: Используя обычный docker-compose.yml**

```bash
docker compose up -d
# или
docker-compose up -d
```

---

### Шаг 3: Проверить что контейнеры запустились

```bash
docker ps
```

**Должно вывести примерно:**
```
CONTAINER ID   IMAGE              STATUS      PORTS
abc123...      parts-system-php   Up 2 mins   0.0.0.0:11000->80/tcp
def456...      parts-system-db    Up 2 mins   3306/tcp
```

---

### Шаг 4: Открыть в браузере

```
http://localhost:11000
```

Должна открыться главная страница Parts System.

---

## 🧪 Проверка

### В браузере (localhost:11000):
- [ ] Страница загружается
- [ ] Видна навигация
- [ ] Нет ошибок 500

### Консоль браузера (F12):
- [ ] CSS подгружаются (Network → *.css = 200 OK)
- [ ] Нет красных ошибок
- [ ] `typeof Alpine === "object"` ✅

### Vite (должен быть запущен отдельно):
```bash
npm run dev
# Должно показать:
# VITE v7.x.x  ready in xxx ms
# ➜  Local: http://localhost:5173/
```

---

## 🚀 Всё вместе (полный старт)

**Терминал 1 (Vite):**
```bash
npm run dev
```

**Терминал 2 (Docker):**
```bash
docker compose -f docker-compose.local.yml up -d
```

**Затем открыть:**
```
http://localhost:11000
```

---

## 🐛 Если всё ещё не работает

### ❌ "Cannot connect to Docker daemon"

**Решение:**
```bash
# Запустить Docker Desktop
open /Applications/Docker.app

# Подождать 30 сек
sleep 30

# Проверить
docker ps
```

### ❌ "Port 11000 already in use"

**Решение:**
```bash
# Найти процесс на порту 11000
lsof -i :11000

# Убить процесс (замени <PID> на номер)
kill -9 <PID>

# Или остановить Docker контейнеры
docker compose down

# И запустить заново
docker compose -f docker-compose.local.yml up -d
```

### ❌ "Контейнеры запустились но страница не загружается"

**Решение:**
```bash
# Проверить логи контейнера
docker compose -f docker-compose.local.yml logs php-fpm

# Или
docker compose logs -f

# Перезагрузить контейнеры
docker compose restart

# Или пересобрать
docker compose -f docker-compose.local.yml down
docker compose -f docker-compose.local.yml up -d
```

---

## 📝 Краткая справка

| Команда | Что делает |
|---------|-----------|
| `docker ps` | Показать запущенные контейнеры |
| `docker compose up -d` | Запустить контейнеры |
| `docker compose down` | Остановить контейнеры |
| `docker compose logs -f` | Смотреть логи в реальном времени |
| `docker compose exec php-fpm bash` | Зайти в PHP контейнер |
| `npm run dev` | Запустить Vite |

---

## ✅ Итог

```
1. Запустить Docker Desktop
2. Запустить docker compose
3. Открыть http://localhost:11000
4. Готово!
```

**Status: FIXED ✅**
