# 🚀 Локальная сборка - Инструкции по запуску

## Проблемы, которые решены в этой версии

### ✅ 1. Удаление картинок для моторов
Теперь в админке при редактировании мотора можно видеть все загруженные изображения и удалять их по клику на кнопку удаления.

**Файлы:**
- `resources/views/vendor/backpack/crud/fields/engine_media_gallery.blade.php` - новый field тип для работы с MediaLibrary
- `app/Http/Controllers/Admin/EnginesCrudController.php` - обновлен field для использования `engine_media_gallery`
- `routes/backpack/custom.php` - маршруты для удаления медиа

### ✅ 2. Исправлена ошибка "base:::email Login" на странице входа в админку
Проблема была в отсутствии русских переводов. Добавлены языковые файлы:
- `resources/lang/vendor/backpack/ru/base.php`
- `resources/lang/vendor/backpack/ru/crud.php`

## 📋 Запуск локальной сборки

### Первый запуск или обновление зависимостей

```bash
# Установка зависимостей npm
npm install

# Сборка фронтенда для production (если нужно)
npm run build
```

### Повседневный запуск (development режим)

**Вариант 1: Использование Docker Compose (рекомендуется)**

```bash
# В первом терминале - запуск сервиса
docker-compose -f docker-compose.local.yml up -d

# Проверка логов
docker-compose -f docker-compose.local.yml logs -f php_local

# Запуск Vite dev сервера (во втором терминале)
npm run dev

# Сейчас приложение доступно на:
# http://localhost:11000 (фронтенд)
# http://localhost:11000/admin (админка)
# http://localhost:5173 (Vite dev сервер)

# Для выключения:
docker-compose -f docker-compose.local.yml down
```

**Вариант 2: Без Docker (используя встроенный PHP сервер)**

```bash
# В первом терминале - запуск Artisan сервера
php artisan serve --host=0.0.0.0 --port=8000

# Во втором терминале - запуск Vite dev сервера
npm run dev

# Сейчас приложение доступно на:
# http://localhost:8000 (фронтенд)
# http://localhost:5173 (Vite dev сервер)
```

## 🎨 Стили и структура

### Если стили не загружаются:

1. **Убедитесь, что Vite dev сервер запущен:**
   ```bash
   npm run dev
   ```

2. **Проверьте `public/hot` файл** - он должен указывать на адрес Vite dev сервера
   ```bash
   cat public/hot
   # Должно вывести: https://localhost:5173 или http://localhost:5173
   ```

3. **Очистите кэш браузера** (Ctrl+Shift+Delete или Cmd+Shift+Delete)

4. **Если все еще не работает, пересоберите фронтенд:**
   ```bash
   npm run build
   # Затем обновите страницу в браузере
   ```

## 📚 CSS файлы проекта

В проекте используются следующие CSS файлы:
- `resources/css/app.css` - Tailwind CSS (база)
- `resources/css/styles.css` - Кастомные стили приложения
- Bootstrap 5.3.0 (CDN) - для компонентов UI

## 🛠️ Основные команды

```bash
# Установка зависимостей
npm install

# Запуск dev сервера (hot reload)
npm run dev

# Production сборка
npm run build

# Очистка кэша Laravel
php artisan cache:clear
php artisan view:clear

# Очистка всех кэшей (в Docker контейнере)
docker exec php_local php artisan cache:clear
docker exec php_local php artisan view:clear
```

## ⚡ Возможные проблемы и решения

### Проблема: "Стили не загружаются на странице входа"
**Решение:** Убедитесь, что Vite dev сервер запущен (`npm run dev`)

### Проблема: "CSRF token mismatch"
**Решение:** 
```bash
php artisan cache:clear
# или в Docker:
docker exec php_local php artisan cache:clear
```

### Проблема: "Изображения не загружаются"
**Решение:** Убедитесь, что папка `storage` имеет правильные права доступа:
```bash
# Если используется Docker:
docker exec php_local chmod -R 777 storage
# Или локально:
chmod -R 777 storage
```

## 📝 Следующие шаги для production

Перед развертыванием на production:

1. Запустите тесты (если есть)
2. Проверьте все функции:
   - ✓ Загрузка картинок для моторов
   - ✓ Удаление картинок (новая функция!)
   - ✓ Страница входа отображается корректно
3. Выполните production сборку:
   ```bash
   npm run build
   ```
4. Убедитесь, что языковые файлы скопированы на продакшене
