# 🔍 Отладка AmoCRM для приватных интеграций

## Проблема
После авторизации токен не сохраняется в БД, команда `php artisan amo:status` показывает "❌ AmoCRM не подключена!"

## Что было исправлено

### 1. Улучшено логирование в callback
- Добавлено детальное логирование всех параметров запроса
- Логируется subdomain и источник его определения
- Проверка сохранения токена в БД

### 2. Улучшено определение subdomain для приватных интеграций
- Проверка параметра `referer` в запросе
- Проверка заголовка `Referer`
- Проверка параметра `account`
- Извлечение subdomain из полного URL если нужно

### 3. Проверка сохранения токена
- После сохранения токена проверяется, что он действительно записался в БД
- Детальное логирование процесса сохранения

## Как проверить

### Шаг 1: Проверьте логи после авторизации

```bash
docker compose exec app tail -f storage/logs/laravel.log
```

Затем перейдите на `/amocrm/install` и авторизуйтесь. В логах должны появиться записи:
- `AmoCRM OAuth: callback получен` - с полными параметрами
- `AmoCRM OAuth: subdomain определен` - какой subdomain был использован
- `AmoCRM успешно авторизована` - токен сохранен

### Шаг 2: Проверьте redirect_uri

Убедитесь, что в настройках интеграции AmoCRM указан правильный redirect_uri:
- В AmoCRM: Настройки → Интеграции → Ваша интеграция → Redirect URI
- Должен быть: `https://avrb1749.ru/amocrm/callback` (или ваш домен)

### Шаг 3: Проверьте переменные окружения

```bash
docker compose exec app php artisan tinker
```

Затем выполните:
```php
config('amocrm.client_id');
config('amocrm.client_secret');
config('amocrm.redirect_uri');
config('amocrm.subdomain');
```

Все должны быть заполнены и совпадать с настройками в AmoCRM.

### Шаг 4: Проверьте БД напрямую

```bash
docker compose exec app php artisan tinker
```

```php
\App\Models\AmocrmToken::all();
```

Если токенов нет - callback не вызывается или не сохраняет токен.

### Шаг 5: Проверьте маршрут callback

Убедитесь, что маршрут `/amocrm/callback` доступен:
```bash
curl -I https://avrb1749.ru/amocrm/callback
```

Должен вернуть 200 или 302 (редирект).

## Возможные проблемы

### Проблема 1: Callback не вызывается
**Признаки:** В логах нет записей "AmoCRM OAuth: callback получен"

**Решение:**
1. Проверьте redirect_uri в настройках интеграции AmoCRM
2. Убедитесь, что redirect_uri точно совпадает (включая протокол https/http)
3. Проверьте, что домен доступен извне

### Проблема 2: Callback вызывается, но токен не сохраняется
**Признаки:** В логах есть "callback получен", но нет "AmoCRM успешно авторизована"

**Решение:**
1. Проверьте логи на ошибки обмена кода на токен
2. Проверьте, что subdomain определяется правильно
3. Проверьте права доступа к БД

### Проблема 3: Subdomain не определяется
**Признаки:** В логах "subdomain не определен"

**Решение:**
1. Для приватных интеграций subdomain может передаваться в параметре `referer` или `account`
2. Убедитесь, что в `.env` указан правильный `AMOCRM_SUBDOMAIN`
3. Проверьте логи - там будет указано, откуда пытался взять subdomain

## Дополнительная отладка

### Включите debug маршрут

В `routes/web.php` уже есть маршрут `/amocrm/callback-debug` который логирует все запросы.

Попробуйте временно изменить redirect_uri в AmoCRM на `/amocrm/callback-debug` и посмотрите, что приходит.

### Проверьте таблицу amocrm_oauth_states

```bash
docker compose exec app php artisan tinker
```

```php
\App\Models\AmocrmOauthState::all();
```

Если там много записей - state не удаляется после использования (проблема в callback).

## Следующие шаги

После исправлений:
1. Очистите старые токены: `\App\Models\AmocrmToken::truncate();`
2. Очистите старые state: `\App\Models\AmocrmOauthState::truncate();`
3. Перейдите на `/amocrm/install` и авторизуйтесь заново
4. Проверьте логи
5. Проверьте статус: `php artisan amo:status`

