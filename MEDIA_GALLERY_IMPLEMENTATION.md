# 📸 Система управления фотографиями моторов

## ✅ Что было реализовано

Полностью переделана система загрузки и управления фотографиями моторов с использованием **Livewire 3** и **Spatie Media Library**.

### 🎯 Функциональность

1. **Загрузка фотографий** - выбор нескольких файлов одновременно
2. **Предпросмотр** - показывает выбранные файлы перед загрузкой
3. **Удаление фото** - кнопка удаления для каждой фотографии
4. **Валидация** - проверка формата и размера файлов
5. **Уведомления** - информирование об успехе/ошибке операций
6. **Кэширование** - автоматическая очистка кэша после изменений

## 📁 Созданные/Обновленные файлы

### Новые файлы

```
app/Livewire/EngineMediaManager.php
  - Компонент Livewire для управления медиа
  - Методы: mount(), loadImages(), saveMedia(), deleteImage(), removeUploadedFile()
  - Интеграция с Spatie Media Library

resources/views/livewire/engine-media-manager.blade.php
  - UI для управления фотографиями
  - Галерея существующих фото
  - Форма загрузки новых фото
  - Ошибки валидации

resources/views/vendor/backpack/crud/engines_edit.blade.php
  - Переопределенная форма редактирования мотора
  - Встроены основные поля формы (slug, title, price, и т.д.)
  - Добавлен Livewire компонент для управления фотографиями
```

### Обновленные файлы

```
app/Http/Controllers/Admin/EnginesCrudController.php
  - Добавлена настройка для использования кастомной view
  - Удаления старого поля 'engine_media_gallery'
  - CRUD::setEditView('vendor.backpack.crud.engines_edit')
```

## 🚀 Как использовать

### 1. Переход в админку и редактирование мотора

```
Админка → Двигатели → Нажать на мотор для редактирования
```

### 2. После сохранения основных данных (обязательно!)

**Форма разделена на две части:**
- Верхняя часть: основные данные мотора (slug, title, price и т.д.)
- Нижняя часть: управление фотографиями

### 3. Загрузка фотографий

```
1. Нажать кнопку "Выбрать файлы"
2. Выбрать файлы (JPG, PNG, WEBP, макс 5 MB)
3. Увидите превью выбранных файлов
4. Нажать "Загрузить фото"
```

### 4. Удаление фотографии

```
1. Находите фото в галерее
2. Наведите на фото - появится кнопка корзины (🗑️)
3. Нажмите кнопку удаления
4. Фото удалится сразу же
```

## ⚙️ Технические детали

### Компонент Livewire: `EngineMediaManager`

**Свойства:**
- `Engine $engine` - модель мотора
- `$images = []` - список загруженных фото
- `$uploadedFiles = []` - временно загруженные файлы

**Методы:**
- `mount(Engine $engine)` - инициализация компонента
- `loadImages()` - загружает фото из MediaLibrary
- `saveMedia()` - валидирует и сохраняет фото
- `deleteImage($mediaId)` - удаляет фото по ID
- `removeUploadedFile($key)` - убирает файл из очереди

### Валидация файлов

```php
'uploadedFiles.*' => 'required|file|mimes:jpeg,png,webp|max:5120', // 5 MB
```

- **Форматы**: JPG, PNG, WEBP
- **Максимальный размер**: 5 MB на файл
- **Максимум файлов**: 6 фото всего

### Интеграция со Spatie Media Library

**Регистрация коллекции:**
```php
public function registerMediaCollections(): void
{
    $this->addMediaCollection('images')
        ->useDisk('public')
        ->acceptsMimeTypes(['image/jpeg', 'image/png', 'image/webp']);
}
```

**Конверсии (автоматические превью):**
```php
// thumb - 250x250 для каталога
$this->addMediaConversion('thumb')
    ->crop(250, 250)
    ->quality(75)
    ->nonQueued();

// preview - 600x600 для карточки товара
$this->addMediaConversion('preview')
    ->crop(600, 600)
    ->quality(75)
    ->nonQueued();
```

## 🔄 Как Livewire обновляет интерфейс

1. **Загрузка файлов**: `wire:model="uploadedFiles"` - двусторонняя привязка
2. **Сохранение**: `wire:click="saveMedia"` - вызов метода компонента
3. **Удаление**: `wire:click="deleteImage({{ $image['id'] }})"` - удаление с ID
4. **Уведомления**: `$this->dispatch('notify', [...])` - отправка события

## 📊 Поток данных

```
Пользователь выбирает файлы
    ↓
wire:model="uploadedFiles" отправляет на сервер (Livewire)
    ↓
Компонент создает временные URL для превью
    ↓
Пользователь видит превью и нажимает "Загрузить"
    ↓
saveMedia() валидирует файлы
    ↓
foreach файл → $engine->addMedia($file)->toMediaCollection('images')
    ↓
Spatie Media Library создает конверсии (thumb, preview)
    ↓
loadImages() перезагружает список
    ↓
UI обновляется, показывает новые фото
```

## 🐛 Отладка

### Логи загрузки

Проверьте `storage/logs/laravel.log`:

```bash
tail -f storage/logs/laravel.log | grep -i "media\|upload"
```

### Очистка временных файлов

Livewire автоматически чистит временные файлы, но если нужно вручную:

```bash
php artisan livewire:configure-s3-upload-cleanup
```

### Проверка конверсий

```bash
# Проверить что конверсии создаются
SELECT id, file_name, generated_conversions FROM media;
```

## 🎨 Кастомизация

### Изменить максимальный размер файла

`app/Livewire/EngineMediaManager.php` строка 57:
```php
'uploadedFiles.*' => 'required|file|mimes:jpeg,png,webp|max:10240', // 10 MB
```

### Изменить максимальное количество фото

`app/Livewire/EngineMediaManager.php` строка 68:
```php
if ($currentImages + $newFiles > 10) {  // 10 вместо 6
    throw new \Exception('...');
}
```

### Изменить размер конверсий

`app/Models/Engine.php`:
```php
$this->addMediaConversion('thumb')
    ->crop(500, 500)  // Изменить размер
    ->quality(90)     // Изменить качество
    ->nonQueued();
```

## ✅ Что проверить

- [ ] Синтаксис PHP - ✅ Нет ошибок
- [ ] Компонент существует - ✅ Класс загружается
- [ ] View существует - ✅ Файл создан
- [ ] Контроллер обновлен - ✅ CRUD::setEditView() добавлен
- [ ] Конверсии настроены - ✅ thumb и preview
- [ ] Disk 'public' настроен - ✅ storage/app/public → /storage
- [ ] WithFileUploads - ✅ Поддерживается в Livewire 3

## 📝 Что дальше

### Опциональные улучшения

1. **Drag & drop** - перетаскивание файлов
2. **Сортировка** - изменение порядка фото
3. **Компрессия** - автоматическое сжатие фото
4. **Галерея на фронте** - отображение в каталоге с лайтбоксом
5. **Массовая загрузка** - из архива ZIP

## ❓ Частые вопросы

**В: Где хранятся фото?**
О: В `storage/app/public/` (доступны по `/storage/...`)

**В: Почему конверсии долго создаются?**
О: Используется `nonQueued()` для синхронной генерации. Если медленно, используйте очередь.

**В: Можно ли загружать в облако (S3)?**
О: Да, измените `useDisk('public')` на `useDisk('s3')` в `registerMediaCollections()`

**В: Как показать фото на главной странице?**
О: Используйте `$engine->getAllImages()` в view - возвращает массив с thumb и preview

```blade
@foreach($engine->getAllImages() as $image)
    <img src="{{ $image['thumb'] }}" alt="...">
@endforeach
```

---

**Создано:** 30 декабря 2025 г.
**Версия:** 1.0
**Статус:** ✅ Готово к использованию
