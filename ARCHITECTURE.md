# 🎯 Архитектура решения

## 📊 Структура обработки изображений

```
┌─────────────────────────────────────────────────────────────┐
│                    АДМИНКА (BACKPACK)                       │
│                                                              │
│  1. Edit Motor → "Фотографии мотора"                       │
│     ├─ Выбрать файлы                                       │
│     └─ Save                                                 │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│              LARAVEL BACKEND (app/Models)                   │
│                                                              │
│  Engine::saved() event:                                    │
│  ├─ addMedia($file) → MediaLibrary                         │
│  └─ registerMediaConversions() →                           │
│     ├─ thumb 250x250 (45 KB)  ←────┐                       │
│     └─ preview 600x600 (120 KB)    │ WebP 75%             │
│                                     │ quality              │
│     Cache::remember('engine_images_*', 24h)               │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│              ФАЙЛОВОЕ ХРАНИЛИЩЕ                             │
│                                                              │
│  storage/app/public/                                        │
│  ├─ 1/ (engine id)                                         │
│  │  ├─ engine-photo.jpg (original 2.5 MB)                 │
│  │  ├─ conversions/                                        │
│  │  │  ├─ engine-photo-thumb.jpg (45 KB)                 │
│  │  │  └─ engine-photo-preview.jpg (120 KB)              │
│  │  └─ ...                                                 │
│  └─ 2/ (другой engine)                                     │
└──────────────────────┬──────────────────────────────────────┘
                       │
        ┌──────────────┴──────────────┐
        │                             │
        ▼                             ▼
┌──────────────────┐        ┌─────────────────────┐
│  NGINX (ВЕББОК)  │        │   БРАУЗЕР ЮЗЕРА     │
│                  │        │                     │
│  ✓ Gzip           │        │ Cache:              │
│  ✓ Cache-Control  │        │  • 30 дней         │
│  ✓ expires 30d    │        │  • LocalStorage     │
│                  │        │  • Service Worker   │
└──────┬───────────┘        └──────────┬──────────┘
       │                              │
       │ /images/           /storage   │
       │ thumb (45 KB)      preview    │
       └──────────────────────────────►
                    
                    КАТАЛОГ ТОВАРОВ
                    (фронтенд)
                    
                    ✓ 1-3 сек загрузка
                    ✓ Использует thumb
                    ✓ Кэш в браузере
```

---

## 🔄 Цикл загрузки и отображения

```
ЗАГРУЗКА (Admin Panel):
═══════════════════════════════════════════════

User chooses image
    │
    ▼
POST /admin/engines/{id}/upload
    │
    ├─ Validate MIME types
    ├─ Store in storage/app/public/
    │
    └─ MediaLibrary processes:
       ├─ Create thumb 250x250
       ├─ Create preview 600x600
       ├─ Save metadata to DB
       └─ Done! (45KB + 120KB per image)


ОТОБРАЖЕНИЕ (Frontend Catalog):
════════════════════════════════════════════════

User opens /catalog
    │
    ▼
Engine::getAllImages() called
    │
    ├─ Check Laravel Cache
    │  └─ HIT? Return cached URLs
    │     └─ MISS? Continue...
    │
    ├─ Query MediaLibrary
    │  └─ Get media collection 'images'
    │
    ├─ Generate URLs:
    │  ├─ $media->getUrl('thumb')      → /storage/1/...thumb.jpg (45KB)
    │  ├─ $media->getUrl('preview')    → /storage/1/...preview.jpg (120KB)
    │  └─ Store in Cache for 24h
    │
    ▼
View returns JSON/HTML
    │
    ├─ Nginx serves with:
    │  ├─ Gzip compression
    │  ├─ Cache-Control headers
    │  └─ Browser caches 30 days
    │
    ▼
Browser displays images
    │
    ├─ Next page load:
    │  └─ From browser cache (0ms)
    │
    ▼
Success!


УДАЛЕНИЕ (Admin Panel):
═════════════════════════════════════════════

User clicks "Удалить" on image
    │
    ▼
AJAX POST /admin/engine/delete-media
    │
    ├─ Find media by ID
    ├─ Delete from storage/app/public/
    ├─ Delete from DB
    ├─ Clear Laravel Cache
    │
    ▼
UI updates immediately
    │
    ├─ No image in list
    ├─ Size on disk reduced
    ├─ No orphaned files
    │
    ▼
Success!
```

---

## 🎨 Размеры и форматы

```
ОРИГИНАЛЬНОЕ ИЗОБРАЖЕНИЕ:
┌──────────────────────────────┐
│                              │  
│     Фотография с камеры      │  
│       или скачанная          │  
│                              │  
│    4000 x 3000 px            │  
│    2.5 MB JPG                │  
│                              │  
└──────────────────────────────┘
           │
           │ registerMediaConversions()
           │
    ┌──────┴────────┐
    │               │
    ▼               ▼
┌────────────┐  ┌──────────────┐
│   THUMB    │  │   PREVIEW    │
│            │  │              │
│ 250 x 250  │  │ 600 x 600    │
│ 45 KB JPG  │  │ 120 KB JPG   │
│ (crop)     │  │ (crop)       │
│            │  │              │
│ Для:       │  │ Для:         │
│ • Списки   │  │ • Карточки   │
│ • Превью   │  │ • Деталей    │
│ • Каталог  │  │ • Просмотра  │
└────────────┘  └──────────────┘

Оригинал хранится в полном размере для:
- Скачивания пользователем
- Увеличения на мобильном
- Будущих дополнительных конверсий
```

---

## 💾 Структура папок

```
storage/app/public/
├── 1/ (engine id = 1)
│   ├── engine-photo-001.jpg (2.5 MB) - оригинал
│   ├── conversions/
│   │   ├── engine-photo-001-thumb.jpg (45 KB)
│   │   └── engine-photo-001-preview.jpg (120 KB)
│   ├── engine-photo-002.jpg (2.3 MB) - оригинал
│   └── conversions/
│       ├── engine-photo-002-thumb.jpg (42 KB)
│       └── engine-photo-002-preview.jpg (115 KB)
│
├── 2/ (engine id = 2)
│   ├── image-123.png (1.8 MB)
│   └── conversions/
│       ├── image-123-thumb.jpg (38 KB)
│       └── image-123-preview.jpg (105 KB)
│
└── ...

public/storage/ → symlink → ↑ (созданный php artisan storage:link)

public/images/engines/ (старая система - все еще поддерживается)
├── d245b4 (по OEM)
│   ├── photo1.jpg
│   ├── photo2.jpg
│   └── ...
└── ...
```

---

## 🔐 Безопасность и лучшие практики

```
┌──────────────────────────────────────┐
│      ЗАГРУЖЕННЫЕ ФАЙЛЫ ЗАЩИТЫ        │
└──────────────────────────────────────┘

1. MIME TYPE VALIDATION
   ┌─────────────────────┐
   │ Загруженный файл    │
   ├─────────────────────┤
   │ MIME: ?             │
   └────────┬────────────┘
            │
            ▼
   ✓ image/jpeg
   ✓ image/png
   ✓ image/webp
   ✗ image/svg (потенциальная угроза)
   ✗ application/pdf (отклоняется)

2. FILE STORAGE SECURITY
   ┌──────────────────────────────────┐
   │ storage/app/public/              │
   │  └─ Вне root, защищено           │
   │     symlink в public/ ← управляемо│
   └──────────────────────────────────┘

3. DATABASE RECORDS
   ┌──────────────────────────────────┐
   │ media table                      │
   ├─────────────────────────────────┤
   │ id | model_id | collection_name  │
   │ 1  | 5        | images           │
   │ 2  | 5        | images           │
   │ 3  | 6        | images           │
   │    | ...      | ...              │
   │                                  │
   │ ✓ Связь с Engine (model_id)     │
   │ ✓ Типизация (collection_name)   │
   │ ✓ Мягкие удаления               │
   └──────────────────────────────────┘

4. CACHE SECURITY
   ┌────────────────────────────────┐
   │ cache:remember($key, ttl)      │
   │                                │
   │ $key = 'engine_images_5'       │
   │ ttl = 86400 (24 часа)          │
   │                                │
   │ ✓ Автоматически инвалидируется│
   │ ✓ Очищается при сохранении    │
   │ ✓ Изолирован по Engine ID      │
   └────────────────────────────────┘
```

---

## 📈 Масштабируемость

```
ТЕКУЩЕЕ РЕШЕНИЕ (<=1000 моторов):
═════════════════════════════════════
┌─────────────────────────────────┐
│ Laravel Cache (File driver)      │
│ ├─ storage/framework/cache/      │
│ └─ Быстро для ~1K записей        │
│                                  │
│ Media Library (Local storage)    │
│ ├─ storage/app/public/          │
│ └─ Для <=100GB данных           │
│                                  │
│ Nginx                            │
│ └─ Справляется с 1K RPS         │
└─────────────────────────────────┘

РЕКОМЕНДАЦИИ ДЛЯ РОСТА:
═════════════════════════════════════
1. 1K-10K моторов:
   └─ Добавить Redis для Cache
   
2. 10K-100K моторов:
   └─ Переместить на S3
   └─ Добавить Image CDN
   
3. 100K+ моторов:
   └─ Микросервис обработки
   └─ Message Queue для конверсий
   └─ Distributed cache (Redis Cluster)
```

---

**Диаграмма версия**: 1.0
**Совместимость**: Laravel 12, Spatie Media Library 10+, Backpack 6.8+
